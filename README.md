# the components folder

put inside the component folder all the components you use in you app
when u backup your own component folder give it a name youname+components
each component you create make it inside a folder that contain it jsx and css 

# the pages folder

in this folder put the pages you have created for the website 
each one makes his pages in one folder named hisname+pages before put it inside the pages folder

# App.js file

make the app.js clean and put inside it just the roots of our pages and the componets that will appear in all pages (like footer and header)

# Assets folder ( in public folder )

put all you images in the assets folder 